<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.LianLianKan"
        android:supportsRtl="true"
        tools:targetApi="31">

        <!-- 主Activity -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 游戏结果Activity -->
        <activity
            android:name=".activity.GameResultActivity"
            android:exported="false"
            android:parentActivityName=".activity.MainActivity" />

        <!-- 版本检查Activity -->
        <activity
            android:name=".activity.VersionCheckActivity"
            android:exported="false"
            android:label="@string/version_check" />

        <activity
            android:name=".activity.AuthActivity"
            android:exported="false"
            android:label="@string/account" />

        <activity
            android:name=".activity.BattleActivity"
            android:exported="false"
            android:label="@string/battle_mode" />

        <!-- 背景音乐Service -->
        <service
            android:name=".service.MusicService"
            android:exported="false"
            android:enabled="true"
            android:foregroundServiceType="mediaPlayback" />

        <!-- 耳机插拔广播接收者 -->
        <receiver
            android:name=".receiver.HeadsetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.HEADSET_PLUG" />
            </intent-filter>
        </receiver>

        <!-- 游戏结果广播接收者 -->
        <receiver
            android:name=".receiver.GameResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.lianliankan.ACTION_GAME_WIN" />
                <action android:name="com.example.lianliankan.ACTION_GAME_RESULT" />
                <action android:name="com.example.lianliankan.ACTION_START_MUSIC" />
                <action android:name="com.example.lianliankan.ACTION_PAUSE_MUSIC" />
            </intent-filter>
        </receiver>

        <!-- 排行榜ContentProvider -->
        <provider
            android:name=".provider.RankProvider"
            android:authorities="com.example.lianliankan.rankprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
